# Components on the left prompt
export MNML_PROMPT=(mnml_ssh mnml_status mnml_keymap)

# Components on the right prompt
export MNML_RPROMPT=('mnml_cwd 2 0' mnml_git)

# Components shown on info line
export MNML_INFOLN=(mnml_err mnml_jobs mnml_uhp)
export MNML_MAGICENTER=(mnml_me_dirs mnml_me_ls mnml_me_git)
